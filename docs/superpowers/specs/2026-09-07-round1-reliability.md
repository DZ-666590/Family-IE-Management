# 第一轮可靠性修复：已批准范围

依据：2026-09-07 用户体验评审及用户“执行第一轮修复”的明确指令。基线 `d6069f4`，当前分支 `codex/clarity-workspace`。

## 必须交付

1. UX-01：总览预算与预算详情统一分/元口径；已有分类汇总规则保持一致，不把重叠预算描述成去重支出。
2. UX-02：3.6%、3.1%、4.9% 等合法利率保存正确，不暴露内部小数表示；不静默舍弃超出允许精度的输入。
3. UX-03：贷款计划能访问第 1、50、51、360 期；检查并补齐其余现有列表/历史的分页，不能仅提高固定上限。引用选择不能因分页而缺失已有选项。
4. UX-04：成功写入后关联列表与总览/净资产/预算/提醒/年度统计更新，不需要浏览器刷新；旧响应不能覆盖新状态。
5. UX-05：日期统一为 Asia/Shanghai 业务日，覆盖凌晨、月末和年末。
6. UX-06：前端可操作能力与现有后端权限一致，尤其成员不能编辑他人流水、不能生成提醒；不改变后端授权规则。
7. UX-07：新表单清除旧操作错误；提交错误在当前任务中可见、可定位，跨步骤错误回到相应步骤。
8. UX-10：有改动的表单关闭/遮罩/Escape/路由切换均有保护；未改动不打断；安全退出或会话失效清除草稿，密码不持久化。

## 约束与完成门槛

- Java 17 / Spring Boot / React / TypeScript / Semi Design，保留现有视觉与业务规则；不增加数据库迁移、资金联动或新的贷款计算策略。
- 复用现有组件及依赖；不得引入未经需要的新框架。
- 保持当前分支，使用已存在的隔离 worktree；保护其他工作区和用户数据。
- 所有新增写入回归使用隔离测试库；线上只读核验，不改现有家庭数据。
- 完成包含本地检查、推送当前分支、GitHub Actions 成功、线上版本和关键页面核验；不把本地提交当部署完成。
- 服务器地址、密钥和数据库配置不写入公共仓库。
