# Stage2 三模块集成

## 合入内容

- 账户细分：`804d1bb`，保留原账户 ID/余额/初始化语义，增加钱包平台、银行名称与卡号后四位。
- 个人 AI 设置：`362d7f3`，用户隔离、密钥加密、HTTPS/允许域名/费用确认等保护。
- 版本化部署：`d7d2a03`，JAR 与行情适配器同包交付、版本核验与成对回退。
- 基线为已合并 PR #6 的 stage2 `9449e30`，不覆盖周期账单新功能。

## 冲突处理

Git 文本自动合并成功，但三个模块存在重复 V26 的语义冲突。已在隔离整合目录复现 Flyway `Found more than one migration with version 26`，并统一为：

| 版本 | 内容 |
|---|---|
| V26 | 已发布周期账单，脚本保持原样 |
| V27 | 账户资料扩展，H2/MySQL 同步 |
| V28 | 个人 AI 设置，H2/MySQL 同步 |

同步更新最新版本断言和完整版本列表，不删除迁移验证。三个原模块 worktree 保留，stage2 用户已有 IDE 文件不纳入提交。

## 验证（2026-09-08）

- 前端类型检查、生产构建通过；44 文件、268 项测试通过。
- 后端单元/MockMVC/迁移等检查共 616 项：614 通过、2 项可选检查跳过，无失败。未运行本地业务启动 smoke。
- 部署与制品检查 24 项通过；行情适配器检查 44 项通过。
- 生产 MySQL 只读确认已执行 V26。V27/V28 在独立、仅含结构的 MySQL 验证库执行成功；该验证库不含生产业务数据，验收后已移除。未提前在生产执行新迁移。
- 已备份生产数据库、原 JAR、接收器、配置和适配器 unit，文件仅保存在服务器受限目录，不放入仓库。
- 已升级接收器并准备 root 管理的版本化 runtime 与 legacy 回退路径。沿用已验证依赖的独立快照，不升级包；记录包版本、原安装报告和逐文件摘要，并验证低权限/jitless/MemoryDenyWriteExecute 兼容。
- 初始化新目录时发现严格 umask 使目录成为 0700；已仅修正新建 runtime/legacy 目录为 0755，再次确认适配器 active、ExecMainStatus=0、原应用 CSRF=200。没有放宽服务沙箱。

## 发布与启用边界

本说明记录合并前验证和服务器接收器准备，不代表新应用版本已经部署成功。推送后由 GitHub Actions 发布完整制品并检查 JAR/适配器版本及港美股行情，失败自动回退文件对。

AI 配置页随代码发布，但现有站点使用 HTTP，服务器尚未配置 AI 存储加密密钥及审核后的服务商域名列表，因此密钥保存/测试仍按设计禁用。启用需要单独完成 HTTPS 与密钥/允许域名配置；本次不关闭这些安全限制，也未调用真实 AI 服务。
